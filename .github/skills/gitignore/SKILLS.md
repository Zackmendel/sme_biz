---
name: gitignore
description: a skill that generates a git ignore file in the projest's root folder when called upon.
---

When user enters gitignore, the agents scans the entire folder structure in the workspace, identifies files and folders that should not be commited to github and generates a gitignore file in the root directory with these files and folders.